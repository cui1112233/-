# Qiantie 仓库版本与开发记忆

> 用途：作为 Qiantie 仓库的长期开发记忆文件。
> 目标：避免新功能误改旧版本、重复开发、分支基线混乱、生产版本无法追溯等问题。
> 建议放置位置：仓库根目录，例如 `QIATIE_REPOSITORY_MEMORY.md`。

---

## 一、当前已经确认的核心问题

Qiantie 当前最大的风险，不是“旧代码存在”，而是：

**仓库没有一个对所有开发者、AI、Codex、部署流程都唯一明确的“当前正式源码主线”。**

这会直接导致：

- 新功能本来应该改新版，却误改旧版。
- 同一功能在不同分支重复实现。
- 某个会话认为 A 是最新版，另一个会话认为 B 是最新版。
- `master`、`release`、`integration`、`feat`、`fix`、`ops` 等分支同时具有“像是当前版本”的特征。
- 搜索功能代码时容易命中旧实现、构建产物、快照、worktree、副本。
- 功能做完后没有稳定合入唯一主线，下一次开发像“没做过一样”。
- 生产环境与 Git 提交无法严格对应时，后续开发基线会失去可信度。

---

## 二、已经确认的仓库现状

### 1. 默认分支仍然可能是旧主线

当前仓库默认分支曾处于 `master`。

但是 V78 相关开发已经分散在大量分支中，包括但不限于：

- `design/...`
- `feat/...`
- `fix/...`
- `hotfix/...`
- `integration/...`
- `ops/...`
- `plan/...`
- `release/...`

这意味着：

**以后不能因为 GitHub 默认打开的是 `master`，就默认认为 `master` 是最新源码。**

---

### 2. V78 分支数量过多

小说获取、批量工厂、豆包执行器、部署等工作同时存在多条：

- 功能分支
- 修复分支
- 集成分支
- 发布分支
- 设计分支
- 部署分支
- 临时分支
- 复制分支

如果没有先确认唯一基线，任何开发都可能从错误版本开始。

---

### 3. 生产版本曾出现无法对应 Git 提交的问题

曾确认 `V78.3.0.3` 的 production snapshot 记录过：

- 找不到准确 Git baseline。
- 生产镜像中的源码与历史 worktree 不一致。
- 生产镜像中的 `app.js` 无法匹配仓库中可达 Git revision。
- snapshot 是内容快照，而不是可靠地从某个正式 Git commit 延伸出来。

这说明过去存在过：

**线上真实版本 ≠ Git 中某个明确正式提交**

的情况。

这个问题属于高优先级版本治理问题。

---

## 三、以后所有开发必须遵守的第一原则

### 唯一主线原则

以后 Qiantie 必须有且只有一个明确的：

**当前正式源码主线**

推荐形式：

- `main`
- 或明确指定的 `release/current`
- 或另一个正式命名，但必须唯一

并且：

**所有新功能、修复、重构都必须从这个唯一主线创建分支。**

禁止：

- 直接从旧 `master` 开始开发。
- 从历史 `feat` 分支继续开发新功能。
- 从 `plan` 分支写代码。
- 从 `ops` 部署分支继续业务功能开发。
- 从 production snapshot 分支长期开发。
- 因为某个分支名字带 `final` 就默认它是最终版本。

---

## 四、每次开始开发前必须执行的检查

任何 AI、Codex 或开发者，在修改代码前必须先完成以下检查：

1. 确认仓库名。
2. 确认当前分支。
3. 确认当前 HEAD SHA。
4. 确认“当前正式源码主线”。
5. 确认当前任务应该修改哪个模块。
6. 检查同一功能是否在其他分支已有更新实现。
7. 检查当前文件是不是旧版、快照、构建产物或重复副本。
8. 确认不是在：
   - `dist`
   - `node_modules`
   - worktree 副本
   - snapshot
   - old/legacy
   - 临时导出目录
   中修改正式源码。

如果无法确认当前基线：

**停止编码，先做版本确认，不允许直接修改。**

---

## 五、新旧版本识别规则

以后搜索到多个相同功能实现时，不能直接选第一个。

必须判断：

- 哪个属于当前正式源码。
- 哪个属于旧实现。
- 哪个属于编译产物。
- 哪个属于历史快照。
- 哪个属于临时设计或演示。
- 哪个属于已经被新版替代的旧页面。

特别是以下模块：

- 小说获取
- 批量工厂
- 生产统一设置
- 发布统一设置
- 版本配置
- 豆包本地执行器
- 121 登录
- AI1 / AI2 / AI3 / AI4 / AI5 生成逻辑
- 后台同步配置

修改前都必须先找到当前正式入口。

---

## 六、版本管理正确方式

### 版本使用 Tag 保存

正式发布版本应使用 Git Tag，例如：

- `v78.3.0.3`
- `v78.3.0.4`
- `v78.4.0`

不要依赖大量长期 `release/xxx` 分支来保存历史版本。

### Branch 用于开发

推荐：

- `feat/...`
- `fix/...`
- `refactor/...`
- `release/...`（短期）
- `hotfix/...`

完成后：

1. 合入唯一主线。
2. 验证。
3. 创建 Tag。
4. 删除或归档临时分支。

---

## 七、必须建立 CURRENT_VERSION 文件

建议仓库根目录增加：

`CURRENT_VERSION.md`

至少记录：

```text
当前产品版本：
当前正式源码分支：
当前正式源码 HEAD：
当前生产部署 Commit：
当前生产镜像：
最后验证日期：

小说获取正式入口：
批量工厂正式入口：
统一设置正式入口：
豆包执行器正式入口：
121 登录正式入口：

禁止修改目录：
已废弃入口：
```

以后每次开发前必须先读这个文件。

---

## 八、禁止直接修改的内容

以下内容原则上不作为正式源码修改对象：

- `node_modules/`
- `frontend/node_modules/`
- `frontend/dist/`
- 其他构建产物
- 临时 debug 文件
- 本地 worktree 副本
- production snapshot 中仅用于恢复/对比的文件
- 已明确标记 deprecated / legacy / old 的旧实现

如果这些目录里出现了看似正确的新功能：

必须先找到对应源代码，而不是直接改构建后的文件。

---

## 九、Worktree 规则

仓库中曾存在 `.worktrees` / `_worktrees` 一类目录风险。

注意：

`.worktrees/` 与 `_worktrees/` 是不同路径。

`.gitignore` 忽略其中一个，并不代表另一个也会被忽略。

以后必须：

- 统一 worktree 目录命名。
- 不把 worktree 副本提交进正式仓库。
- 搜索代码时排除 worktree。
- 不从 worktree 中的旧副本判断“当前功能状态”。

---

## 十、功能完成的定义

以后不能以“某个分支里写完了”作为功能完成。

一个功能只有满足以下条件才算真正完成：

1. 功能代码完成。
2. 测试通过。
3. 确认修改的是当前正式源码。
4. 合入唯一主线。
5. 唯一主线重新验证。
6. 当前版本文件已更新。
7. 如需上线，生产部署对应到明确 Git SHA。
8. 不存在“线上代码无法对应 Git commit”的情况。

否则只能叫：

**分支实现完成**

不能叫：

**Qiantie 已完成该功能。**

---

## 十一、AI / Codex 工作规则

以后任何 AI 或 Codex 接到 Qiantie 开发任务时：

### 必须先做

- 读取当前主线。
- 读取 `CURRENT_VERSION.md`。
- 获取最新 HEAD。
- 查找当前模块真实入口。
- 检查是否已有更新实现。

### 禁止做

- 根据聊天记忆猜当前版本。
- 根据分支名称猜最新版。
- 看到 `final` 就当最终版。
- 默认在 `master` 修改。
- 在旧页面重新实现新版已有功能。
- 为了“先跑起来”直接改 `dist`。
- 功能写完却不进入正式主线。

---

## 十二、遇到“这个功能我之前不是做过了吗？”时的处理方式

不要马上重新开发。

必须先检查：

1. 之前实现在哪个分支。
2. 是否已经合入当前主线。
3. 是否被后续旧基线覆盖。
4. 是否只存在于前端但后端没合。
5. 是否只存在于旧页面。
6. 是否生产版本包含该功能。
7. 是否有同名但不同实现。

只有确认当前正式源码中确实不存在，才允许重新实现。

优先策略应为：

**找回 → 对比 → 合并 → 修正**

而不是：

**重新写一遍。**

---

## 十三、当前建议的仓库治理顺序

暂时不要直接大规模删除分支。

正确顺序：

### 第一阶段：盘点

逐条分类当前 V78 分支：

- 当前主线候选
- 需要合并的新功能
- 已完成历史分支
- 纯设计
- 纯计划
- 纯部署
- production snapshot
- 可删除
- 暂时不能删除

### 第二阶段：确定唯一基线

选出当前最完整源码。

对齐：

- 小说获取
- 批量工厂
- 豆包执行器
- 统一设置
- 版本配置
- 121 登录
- 当前生产功能

### 第三阶段：建立正式主线

建立唯一开发基线，例如：

`main`

并设为 GitHub 默认分支。

### 第四阶段：补版本记录

建立：

- `CURRENT_VERSION.md`
- 正式 Tag
- 部署 SHA 记录

### 第五阶段：清理

确认所有功能已经安全合入后，再：

- 删除旧临时分支
- 删除重复分支
- 清理 worktree
- 清理构建产物
- 清理错误提交的依赖目录
- 清理无价值 snapshot 副本

---

## 十四、当前最重要的记忆

以后遇到 Qiantie 仓库任务，始终记住：

> **不要问“哪个分支看起来最新”，要确认“哪个提交被正式定义为当前源码”。**

> **不要因为旧版本存在就删除它，先确认新版本是否完整继承了旧版本需要保留的功能。**

> **不要在错误基线修功能。**

> **不要让同一功能在多个长期分支各自继续进化。**

> **生产环境必须能够对应到唯一 Git SHA。**

> **所有新开发最终必须回到唯一主线，否则下一次仍然会重复开发。**

---

## 十五、当前用户偏好

用户不是专业 Git / 软件工程人员。

解释仓库问题时：

- 用简单中文。
- 明确告诉用户“这样做会发生什么”。
- 不只报技术名词。
- 如果用户的想法有风险，要直接指出。
- 执行清理、删除、重写前必须先保护现有成果。
- 优先避免重复开发和误删。
- 版本治理应尽量自动化，而不是要求用户记住复杂 Git 操作。

---

## 十六、后续建议追加的强制保护

后续可以继续增加：

- CI 检查：禁止从错误基线发布。
- CI 检查：生产镜像必须写入 Git SHA。
- 分支保护：正式主线禁止直接强推。
- PR 模板：要求填写来源基线和目标版本。
- 自动生成版本信息。
- 页面底部显示当前 build SHA。
- 后台显示当前部署版本。
- Codex 启动检查脚本。
- 仓库入口文档。

最终目标：

**任何人打开仓库 30 秒内，都能知道当前正式版本、当前代码入口、当前生产 Commit，以及应该从哪里继续开发。**

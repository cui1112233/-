name: V88 Host Node Cutover (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain replacement release path
        run: |
          echo 'Retired: host-stage V88 cutover is not a public deployment path.'
          echo 'Deploy only paired immutable images through the unified Docker runner.'
          exit 64

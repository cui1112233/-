name: V88 Linux AMD64 Public Image Release (Legacy Manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-release-retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain current release path
        shell: bash
        run: |
          echo "Automatic image publishing for V88 is retired."
          echo "Use the Git-direct V88 staging and cutover workflows instead."

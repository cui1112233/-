# 更新日志

## 更新序号：2026-08-19-1

日期：2026-08-19

### 一、CM 灵动陪伴（宠物）功能

- **陪伴台词调度器**：CM 宠物按早晚时间段随机主动说话，含冷却与重启保护，用户可开启/关闭。
- **空闲与点击互动台词**：空闲时随机“摸鱼”台词；点击宠物（非拖拽时）随机点击台词并打开聊天，聊天已开则不重复。
- **任务状态反馈**：任务进行中/成功/失败优先展示对应台词，不被主动说话开关抑制。
- **设置开关**：设置页新增“宠物主动说话”本地开关，关闭后停止主动台词与点击互动，任务反馈保留；按用户隔离存储。
- **气泡生命周期**：普通气泡约 7 秒自动消失；页面隐藏时清除气泡与定时器，恢复可见后重新调度；卸载与账号切换清理定时器。

### 二、分镜模式强制基础设定

- 选择“分镜模式”时，无论连续开头、爆款开头或分段开头，每个分镜统一输出 `【基础设定】生成视频不带字幕 | 9:16`、人物行（最多三人）与场景环境行。
- 人物描述由服务端按“基本体征→五官与妆容→发型与发饰→服饰与配饰”从已提取数据组装，缺失字段不编造。
- 模型遗漏头部时，服务端自动为每个分镜补齐，且不破坏镜头画面、对白、时间码与约束设置；画布/剧情/剧本模式不受影响。
- 修复场景地点字段错配、模型自写人物行与固定头部重复、分镜标题误判等边界问题。

### 三、运行与数据

- 新增 `start.bat` 双击启动脚本。
- 用户数据（账号 scrypt 哈希、会话、系统预设、各用户任务与生成历史）纳入 git 跟踪；API 密钥仍保持忽略不入库。
- 补装 `https-proxy-agent` 依赖（上游代码引用但未登记，缺失会导致服务端无法启动）。

### 四、同步的远端更新（合并自上游 master）

- **模型连接测试**：文本/图片连接测试独立路由，测试不会保存配置；模型目录 Base URL 规范化。
- **模型中心**：兼容性数据结构与凭据管理、模型可见性角色控制。
- **账号图片提供方设置**：设置页新增“生图服务”区（OpenAI 兼容/自定义供应商），独立于文本模型的地址与密钥。
- **水货（Shuihuo）**：资产生成与实时控件相关后端与设计文档、落水可用工具审计。
- **解说工作台**：设计文档。
- **系统预设槽位所有权**：设计文档。
- **上游代理支持**：请求可走代理设置。

### 已知待办（非本次引入）

- `account-image-settings-ui-contract` 中“Shuihuo 图片模型选择器保留账号模型 ID 为 0”的契约测试在远端 master 上先行提交，对应前端实现（`typeof model?.id === 'number'`）尚未合入 master，测试暂为失败状态，等待相关分支合入。

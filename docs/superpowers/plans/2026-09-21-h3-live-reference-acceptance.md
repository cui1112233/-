# H3 实站对照证据与未完成验收

## 采样方式与边界

2026-09-21，通过已登录浏览器只读查看 http://10.0.10.115:8088/ 任务详情、最终 Prompt、Trace 和任务级配置。没有应用修改、重生或提交模型。未使用本地 H3 源码替代实站行为。

目前只冻结了历史任务的一张完整导演卡，**不是完整任务 fixture，也不是一致性验收通过**。真实模型同文对照、完整 Prompt 样本、全量配置、供应方实际提交对照尚待补齐。凭据、签名媒体 URL、客户端 ID 与主机文件路径不进入 fixture。

## 历史输出样本

- 任务：7686714958893042712，源修订 1；完成时间 2026-09-20 15:36:04。
- Trace 的 line_duration_plan 版本：v1.2.25；算法 duration.text-weight。
- 质量配置标识：v78.3.0.59-normal-h3。此标识不是服务部署 SHA；部署 SHA 未取得。
- 视频取文：第 1 个有效行起，共 10 行。配音：zh-CN-XiaochenNeural / general / speed 1.8 / pitch 10。
- 外形附加要求：都要带点妆容；AI 自动人物名单；分镜补充要求和必须细节为空。
- Workflow：minimax_h3_lightx2v_no_pic；768p 竖；轮询 3 秒。
- 配音实测 25.512 秒。第 1 段来源卡 1–5，Canonical 13.298 秒，Request 14 秒；第 2 段来源卡 6–10，Canonical 12.214 秒，Request 12 秒。
- 人物外形 Trace 实际有 3 次模型请求；批量工厂按用户明确要求保持一次请求返回全部人物，这是允许差异。
- 最终 Prompt 顺序：detailed_description → subject_definitions → 视听呈现 → Scene/Total duration/Shot → Audio/Soundscape → H3画面约束。
- 最终人物标记使用稳定 Subject 编号；视觉包含动作五阶段、逐人位置/朝向/视线/动作/反应/结束位置、道具变化、光影、结束状态；声音为环境与拟音；可见人物闭口。
- 第 1 段实际场景时长：3.036、3.463、2.902、2.501、2.098 秒，合计 14 秒；不是 Canonical 13.298 秒。

冻结文件：backend/internal/batchfactoryv11/testdata/h3-live-20260920-card1.json。
内容来自 Trace `artifact: director_chunk_0001 / outline.batch.system` 的第一张卡，字段和值保持原样，仅格式化 JSON。
SHA256：ca6cf3c9080955d9b7f6bd92c0663fb85347874cd06eeb9b625c181324791319。

## 最新任务揭示的版本差异（需确认时长基准）

- 任务：7687502684106001470；完成 2026-09-21 01:22:54。
- Trace 的 line_duration_plan 版本：v1.2.37；method=per_line_tts_probe。
- 配音配置同上，取 10 行。整段 narration_audio_seconds=20.472；逐行合计 timeline_total_seconds=21.264。
- 逐行 ffprobe 秒数：3.240、1.680、1.776、2.328、1.248、3.216、2.352、1.416、1.800、2.208。临时音频 audio_retained=false。
- 最终视频请求 13 秒（卡 1–6）与 8 秒（卡 7–10）。
- 当前页面说明：新任务每行单独临时配音实测，整段音频仅作下载资产，不再按全文权重分配。

这与先前“整段配音真实时长 + 权重分配”的实现不同，会增加配音调用，改变时间轴和分段。用户随后明确回复“按照这个执行”，确认以最新 v1.2.37 逐行实测规则为准。

## 逐行实测接入进度（用户确认后）

- 新前端编译入口按冻结导演卡 source_text 逐行合成，音频字节交给后端 ffprobe；不采用浏览器时长或模型给出的整行权重。
- 后端保存 source_key/source_text_hash/audio hash/逐行毫秒数/总毫秒数及配音配置摘要。任一行缺失、错序、正文过期、无效时长都拒绝，不回退估算。
- 时间轴逐卡直接使用实测值，仅行内微镜头按语义权重分配；分段器接受新 allocator，VIDEO 编译测试通过。
- 复用条件为相同视频原文修订与 hash、逐行来源及配音配置摘要。只改提示词预设或约束开关不会重新调用配音；修改配音配置会重测。
- 整段配音入口未移除，独立资产不被逐行时长覆盖。旧时间轴分配器仍可读取历史数据。
- 新增迁移 1200104，measurement_json 保存逐行证据，NULL 兼容旧记录。迁移尚未在运行数据库执行。
- 本轮失败测试覆盖旧算法改写行时长、HTTP 不接受逐行音频、MySQL 重读丢失逐行数据、分段器拒绝新版本及 API 请求结构；修复后 Go 三包、16 项 Node 行为/API/路由测试通过，前端构建通过。
- 原前端源码测试中“写整段配音时长再编译”的断言不再符合确认规则，替换为测量保存成功后才返回可编译身份、保存失败阻断的行为测试；原有 35 项失败仍保留，105 项中 70 通过。未用删除测试掩盖问题。
- 未验证真实 TTS 提交、运行环境 ffprobe、真实 MySQL 迁移及浏览器完整链；未提交或部署，不宣称页面已生效。

## 本次 TDD 增量

1. 冻结卡 round-trip 首测失败：scene_description、participants、action_beats、continuity_in/out、ambient/foley 等丢失；补充有类型结构后通过。仅说明结构能够保留，不代表新导演预设已完整产生、校验并渲染这些字段。
2. 完整人工 Prompt 的 Trace 首测失败：仍报告 storyboard_facts/output_constraints/visual_baseline/asset_settings 自动注入；改为无自动注入，保留 user_final_prompt 来源。
3. 未解析人物 participant 首测失败：未知 slot 被接受；解析时增加 roster 引用校验。

## 未完成的发布门槛

- 全任务脱敏 fixture 与完整最终 Prompt 对照；最新/历史时长基准选择。
- 扩展导演字段的新写入契约、预设、渲染和连续性逐项覆盖；引用交叉一致性。
- 实际使用资产 ID/修订/正文快照；有效音频测量复用。
- 编译版本检查、保存和卡片更新数据库原子性；并发冲突和手动覆盖确认浏览器验收。
- 35 项前端失败逐项归因，以及单书/批量/重试/合并/打包/视频管理系统上传验收。
- 真实模型同文对照、供应方提交与回收记录。
- 保留脏改动并审查任务增量后，形成 v88 可追溯提交，再部署；本次未提交、未部署。
